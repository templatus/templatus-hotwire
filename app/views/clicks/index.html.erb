<%= render PageTitle::Component.new('Hello, Templatus!') do %>
  <%# nowrap keeps the product names from breaking across lines %>
  An opinionated template for web applications with
  <b class="text-base-content font-semibold whitespace-nowrap">Ruby on Rails</b> and
  <b class="text-base-content font-semibold">Hotwire</b>.
<% end %>

<%= render Clicks::Component.new(clicks: @clicks,
                                 count: @clicks_count,
                                 next_url: @pagy.page_url(:next)) %>
