nav class="flex space-x-5 text-lg font-medium text-center uppercase lg:flex-col lg:space-y-5 lg:space-x-0"
  - tabs.each do |tab|
    = link_to tab.text,
                  tab.href,
                  class: [ 'rounded-md px-2 py-1 transition lg:shadow',
                           current_page?(tab.href) ? 'bg-white text-primary lg:bg-primary lg:text-white' : 'bg-transparent text-white hover:scale-105 lg:bg-white lg:text-primary' \
                         ],
                  data: { turbo_action: 'replace' }
